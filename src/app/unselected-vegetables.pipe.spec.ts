import { UnselectedVegetablesPipe } from './unselected-vegetables.pipe';

describe('UnselectedVegetablesPipe', () => {
  it('create an instance', () => {
    const pipe = new UnselectedVegetablesPipe();
    expect(pipe).toBeTruthy();
  });
});
